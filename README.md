# platform_repos/harnesses / goose

A **future-option vendored harness**. Registered in `tinyhat_harness`
but NOT the active runtime — tinyhat v1 runs on the OpenAI Agents
SDK + sandbox composition (`agents-sdk-python`); Goose waits for its
per-harness runner adapter (v1.x). See PR #120 follow-up issues.

Pinned first-commit payload for the Goose Tinyhat harness mirror.

System repo:
`github.com/tinyloophub/tld--sys--tinyhat-harness-goose`

Upstream:
`github.com/aaif-goose/goose` at `v1.32.0`
(`14a8815746e3c6ffad107f4983df50fd631ec2ce`)

Files in this directory are the Tinyhat-authored manifest,
compliance metadata, license, SBOM placeholder, and empty patch area.
The upstream source tree is mirrored in the managed system repo, not
in the main Tinyloop repository.
